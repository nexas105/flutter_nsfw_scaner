# Dependencies for tools/convert_models.py — install in .venv/ (the
# CoreML venv). For TFLite/Android, see tools/requirements-tflite.txt
# and use a SEPARATE venv (.venv-tflite/) to avoid LLVM CLI flag clashes
# between tensorflow and jax (transitive via litert-torch).
#
# Setup:
#     python3 -m venv .venv
#     .venv/bin/pip install -r tools/requirements.txt
torch>=2.1,<3.0
transformers>=4.36,<6.0
coremltools>=7.2,<9.0
huggingface-hub>=0.20
pillow>=10.0
numpy>=1.24
