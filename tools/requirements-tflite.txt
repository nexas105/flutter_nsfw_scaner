# Dependencies for tools/convert_tflite.py — install in a SEPARATE venv
# (.venv-tflite/) from the CoreML one. litert-torch transitively pulls
# jax, which conflicts with tensorflow's LLVM CLI flag registration when
# both are loaded into the same Python process.
#
# Setup:
#     python3 -m venv .venv-tflite
#     .venv-tflite/bin/pip install -r tools/requirements-tflite.txt
torch>=2.1,<3.0
transformers>=4.36,<6.0
litert-torch>=0.9,<1.0     # PyTorch → TFLite (Google's official path)
ai-edge-quantizer>=0.6     # INT8 weight-only post-quantization
huggingface-hub>=0.20      # for `hf download`
