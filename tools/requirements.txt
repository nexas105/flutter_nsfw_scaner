# Pinned to versions known to round-trip ViT → CoreML cleanly.
# Bump deliberately; coremltools < 7.2 has known issues with FP16 ViT conversion.
torch>=2.1,<3.0
transformers>=4.36,<5.0
coremltools>=7.2,<9.0
huggingface-hub>=0.20
pillow>=10.0
numpy>=1.24
